import streamlit as st
import streamlit.components.v1 as components

st.set_page_config(
    page_title="K-치킨 레스토랑 타이쿤", 
    layout="centered"
)

# 긴 문자열 오류를 차단하기 위해 30~50자 내외로 쪼개어 결합합니다.
h = "<!DOCTYPE html><html><head><meta charset='UTF-8'>"
h += "<style>"
h += "body { font-family: sans-serif; "
h += "background-color: #1a1a24; margin: 0; "
h += "padding: 5px; text-align: center; "
h += "color: #fff; user-select: none; }"
h += ".wrapper { background: #252632; "
h += "padding: 10px; border-radius: 16px; "
h += "border: 3px solid #3b3d54; "
h += "display: inline-block; width: 350px; "
h += "box-sizing: border-box; }"
h += "h1 { color: #ffd43b; margin: 0 0 5px 0; "
h += "font-size: 18px; "
h += "text-shadow: 2px 2px 0px #e67e22; }"
h += ".hud { display: flex; "
h += "justify-content: space-between; "
h += "background: #1c1c24; padding: 6px 12px; "
h += "border-radius: 8px; font-size: 13px; "
h += "font-weight: bold; color: #51cf66; "
h += "margin-bottom: 8px; }"

# 🎮 2D 그래픽 게임 화면 구현 구역
h += ".stage { background-color: #e9ecef; "
h += "border: 3px solid #1a1a24; "
h += "border-radius: 10px; height: 180px; "
h += "position: relative; overflow: hidden; "
h += "margin-bottom: 8px; }"

# 주방 구역 인테리어
h += ".kitchen { position: absolute; left: 0; "
h += "top: 0; width: 90px; height: 100%; "
h += "background: #ced4da; "
h += "border-right: 3px dashed #495057; }"
h += ".k-title { font-size: 9px; color: #495057; "
h += "font-weight: bold; background: #adb5bd; "
h += "padding: 2px 0; }"

# 오브젝트 비주얼 디자인
h += ".chef { position: absolute; left: 30px; "
h += "top: 25px; font-size: 28px; "
h += "transition: transform 0.1s; }"
h += ".fryer-machine { position: absolute; "
h += "left: 15px; top: 85px; width: 60px; "
h += "height: 45px; background: #7f8c8d; "
h += "border: 2px solid #333; border-radius: 6px; "
h += "font-size: 11px; font-weight: bold; "
h += "color: #fff; display: flex; "
h += "flex-direction: column; "
h += "align-items: center; "
h += "justify-content: center; cursor: pointer; }"
h += ".fryer-boil { background: #f39c12 !important; "
h += "animation: shake 0.1s infinite alternate; }"
h += "@keyframes shake { "
h += "  0% { transform: translate(0, 0); }"
h += "  100% { transform: translate(1px, 2px); }"
h += "}"

# 테이블 홀 구역 인테리어
h += ".hall { position: absolute; right: 0; "
h += "top: 0; width: 250px; height: 100%; "
h += "background: #f1f3f5; }"
h += ".table { position: absolute; width: 50px; "
h += "height: 50px; background: #d9480f; "
h += "border-radius: 50%; display: flex; "
h += "flex-direction: column; "
h += "align-items: center; "
h += "justify-content: center; "
h += "font-size: 11px; font-weight: bold; "
h += "color: white; border: 2px solid #821c00; }"
h += ".t1 { left: 30px; top: 20px; }"
h += ".t2 { left: 140px; top: 20px; }"
h += ".t3 { left: 30px; top: 100px; }"
h += ".t4 { left: 140px; top: 100px; }"

# 손님 말풍선 및 아이콘
h += ".guest { position: absolute; top: -22px; "
h += "font-size: 24px; }"
h += ".bubble { position: absolute; top: -45px; "
h += "background: white; color: black; "
h += "font-size: 9px; padding: 2px 4px; "
h += "border-radius: 5px; border: 1px solid #333; "
h += "font-weight: bold; white-space: nowrap; }"

# 컨트롤러 패널 구역
h += ".pbar { width: 80%; background: #333; "
h += "height: 5px; border-radius: 3px; "
h += "overflow: hidden; margin-top: 2px; }"
h += ".pfill { height: 100%; width: 0%; "
h += "background: #2ecc71; }"
h += ".control-panel { background: #1c1c24; "
h += "padding: 8px; border-radius: 8px; "
h += "display: flex; gap: 6px; "
h += "flex-direction: column; }"
h += ".action-btn { background: #f76707; "
h += "color: white; border: none; padding: 8px; "
h += "font-weight: bold; font-size: 13px; "
h += "border-radius: 6px; cursor: pointer; "
h += "border-bottom: 3px solid #b84d00; }"
h += ".action-btn:active { transform: translateY(2px); "
h += "border-bottom: 1px solid #b84d00; }"
h += ".serve-row { display: flex; gap: 4px; "
h += "justify-content: space-between; }"
h += ".s-btn { background: #37b24d; "
h += "border-bottom: 3px solid #2b8a3e; flex: 1; "
h += "font-size: 11px; padding: 6px; color: #fff; "
h += "border-none; font-weight: bold; "
h += "border-radius: 4px; cursor: pointer; }"
h += ".s-btn:active { transform: translateY(1px); }"
h += "</style></head>"

# 🏪 상층 프레임 인터페이스 구축
h += "<body><div class='wrapper'>"
h += "<h1>🍗 리얼 매장 타이쿤 Master</h1>"
h += "<div class='hud'>"
h += "<div>보유 금액: <span id='m'>0</span>₩</div>"
h += "<div>들고있는 치킨: <span id='inv'>0</span>개</div>"
h += "</div>"

# 실시간 모니터링 애니메이션 무대 구조
h += "<div class='stage'>"
h += "<div class='kitchen'>"
h += "<div class='k-title'>KITCHEN</div>"
h += "<div class='chef' id='my-chef'>👨‍🍳</div>"
h += "<div class='fryer-machine' id='machine' onclick='startFry()'>"
h += "<span>튀김기</span>"
h += "<div class='pbar'><div class='pfill' id='f-progress'></div></div>"
h += "</div>"
h += "</div>"

h += "<div class='hall'>"
# 4개의 유기적인 테이블 세트 배치
h += "<div class='table t1' id='table1'>T-1<div id='g1' class='guest'></div><div id='b1' class='bubble'></div></div>"
h += "<div class='table t2' id='table2'>T-2<div id='g2' class='guest'></div><div id='b2' class='bubble'></div></div>"
h += "<div class='table t3' id='table3'>T-3<div id='g3' class='guest'></div><div id='b3' class='bubble'></div></div>"
h += "<div class='table t4' id='table4'>T-4<div id='g4' class='guest'></div><div id='b4' class='bubble'></div></div>"
h += "</div>"
h += "</div>"

# 서빙 가이드 및 인터페이스 버튼 패널
h += "<div class='control-panel'>"
h += "<button class='action-btn' onclick='startFry()'>🔥 튀김기 가동 (치킨 생산)</button>"
h += "<div class='serve-row'>"
h += "<button class='s-btn' onclick='serveTo(1)'>1번 서빙</button>"
h += "<button class='s-btn' onclick='serveTo(2)'>2번 서빙</button>"
h += "<button class='s-btn' onclick='serveTo(3)'>3번 서빙</button>"
h += "<button class='s-btn' onclick='serveTo(4)'>4번 서빙</button>"
h += "</div>"
h += "</div>"

h += "</div>"

# 🕹️ 완벽하게 튜닝된 테이블 타이쿤 AI 스크립트 엔진
h += "<script>"
h += "let money = 0;"
h += "let inventory = 0;"
h += "let fryerState = 'idle'; " # idle -> cooking -> ready
h += "let fryProgress = 0;"
h += "let frySpeed = 8;"

# 테이블 정보 객체 관리
h += "let tables = {"
h += "  1: { active: false, state: 'none', timer: 0, char: '' },"
h += "  2: { active: false, state: 'none', timer: 0, char: '' },"
h += "  3: { active: false, state: 'none', timer: 0, char: '' },"
h += "  4: { active: false, state: 'none', timer: 0, char: '' }"
h += "};"

h += "const guests = ['👨‍💼', '👩‍⚕️', '🐱', '👧', '🦊'];"

h += "function refresh() {"
h += "  document.getElementById('m').innerText = money.toLocaleString();"
h += "  document.getElementById('inv').innerText = inventory;"
h += "}"

# 1. 치킨 조리 루틴
h += "function startFry() {"
h += "  if (fryerState === 'idle') {"
h += "    fryerState = 'cooking';"
h += "    document.getElementById('machine').className = 'fryer-machine fryer-boil';"
h += "    fryProgress = 0;"
h += "    let t = setInterval(function() {"
h += "      fryProgress += frySpeed;"
h += "      if(fryProgress > 100) fryProgress = 100;"
h += "      document.getElementById('f-progress').style.width = fryProgress + '%';"
h += "      if(fryProgress >= 100) {"
h += "        clearInterval(t);"
h += "        fryerState = 'ready';"
h += "        document.getElementById('machine').className = 'fryer-machine';"
h += "        document.getElementById('machine').style.background = '#e67e22';"
h += "      }"
h += "    }, 100);"
h += "  } else if (fryerState === 'ready') {"
h += "    inventory += 1;"
h += "    fryerState = 'idle';"
h += "    document.getElementById('machine').style.background = '#7f8c8d';"
h += "    document.getElementById('f-progress').style.width = '0%';"
h += "    refresh();"
h += "  }"
h += "}"

# 2. 손님 매장 방문 AI 루프
h += "setInterval(function() {"
h += "  let id = Math.floor(Math.random() * 4) + 1;"
h += "  if (!tables[id].active && Math.random() > 0.4) {"
h += "    tables[id].active = true;"
h += "    tables[id].state = 'waiting';"
h += "    tables[id].char = guests[Math.floor(Math.random() * guests.length)];"
h += "    document.getElementById('g' + id).innerText = tables[id].char;"
h += "    let b = document.getElementById('b' + id);"
h += "    b.style.display = 'block';"
h += "    b.innerText = '치킨 줘!';"
h += "  }"
h += "}, 2000);"

# 3. 배달 및 계산 루틴
h += "function serveTo(id) {"
h += "  if (tables[id].active && tables[id].state === 'waiting' && inventory > 0) {"
h += "    inventory -= 1;"
h += "    tables[id].state = 'eating';"
h += "    document.getElementById('b' + id).innerText = '냠냠 🍗';"
h += "    refresh();"
h += "    setTimeout(function() {"
h += "      money += 4500;"
h += "      tables[id].active = false;"
h += "      tables[id].state = 'none';"
h += "      document.getElementById('g' + id).innerText = '';"
h += "      document.getElementById('b' + id).style.display = 'none';"
h += "      refresh();"
h += "    }, 2500);"
h += "  }"
h += "}"

h += "refresh();"
h += "</script></body></html>"

components.html(h, height=450)
